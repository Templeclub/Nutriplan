// Point d'entrée unique pour htm+preact (permet de changer l'implémentation
// sans toucher tous les composants).
export { html } from 'htm/preact';
export { h, render, Fragment, Component } from 'preact';
